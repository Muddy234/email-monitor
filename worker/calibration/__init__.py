"""Post-onboarding calibration layer.

Tests the synthesized personality profile against the user's actual sent
email history, scores the delta, generates correction rules from hard
misses, and iterates until exit thresholds are met.
"""
